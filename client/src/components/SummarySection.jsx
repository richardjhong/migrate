import React from "react"
import '../pages/Form/Form.scss';

export let SummarySection = ({ selections }) => {
    //AFRICA -FOOD QUALITY
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Food Quality') {
        return (
            <div>
                <h1>Ghana</h1>
                <p>Ghana is a beautiful country in West Africa</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Clean Water') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Access to Housing') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Safety') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Free Press') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Healthcare') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Environmental Quality') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Individual Rights') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Personal Freedom and Choice') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Inclusiveness') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Access to Advanced Education') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    //AFRICA -CLEAN WATER
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Food Quality') {
        return (
            <div>
                <h1>Ghana</h1>
                <p>Ghana is a beautiful country in West Africa</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Clean Water') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Access to Housing') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Safety') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Free Press') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Healthcare') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Environmental Quality') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Individual Rights') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Personal Freedom and Choice') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Inclusiveness') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
    if (selections.first === "Africa" && selections.second === 'Food Quality' && selections.third === 'Access to Advanced Education') {
        return (
            <div>
                <h1>Filler</h1>
                <p>Fill</p>
            </div>
        )
    }
}